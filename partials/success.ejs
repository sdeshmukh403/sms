
<% if (typeof msg != "undefined" && msg != null && msg != '') { %>
    <div class="alert alert-success" >
        <strong>Success!</strong> <%= msg %>
      </div>
    <% } %>
    <span class="json-msg"></span>